<div class="flexrow">
    {{~#if displaySelection}}
    <div class="count {{#if invalid}}invalid {{/if}}">
        {{{ localize "sacramento-rpg.CompendiumBrowser.Selection.Label" summary=summary }}}
    </div>
    <button type="submit"{{#if invalid}} disabled data-tooltip aria-label="{{invalidTooltip}}" {{/if}}>
        {{ localize "sacramento-rpg.CompendiumBrowser.Selection.Select" }}
    </button>
    {{/if~}}
</div>
